/*
	string cosa = "prueba/cosa/cosa2.asd";
	char* cosap;
	vector<string> cosa2;
	strcpy(cosap, cosa.c_str());
	cosa2 = split(cosap, "/");
	for(int i=0;i<cosa2.size()-1;i++)
	{
		//(void)CreateDirectory(cosa2[i].c_str(), NULL);
	}
*/
/*
vector<string> split(char* str,const char* delim)
{
    char* token = strtok(str,delim);

    vector<string> result;

    while(token != NULL)
    {
        result.push_back(token);
        token = strtok(NULL,delim);
    }
    return result;
}
*/
/*
// color defines
#define CONSOLE_BLACK = 0;	
#define CONSOLE_BLUE = 1;	
#define CONSOLE_GREEN = 2;	
#define CONSOLE_AQUA = 3;	
#define CONSOLE_RED = 4;	
#define CONSOLE_PURPLE = 5;	
#define CONSOLE_YELLOW = 6;	
#define CONSOLE_WHITE = 7;	
#define CONSOLE_GRAY = 8;
#define CONSOLE_LIGHTBLUE = 9;
#define CONSOLE_LIGHTGREEN = 10;
#define CONSOLE_LIGHTAQUA = 11;
#define CONSOLE_LIGHTRED = 12;
#define CONSOLE_LIGHTPURPLE = 13;
#define CONSOLE_LIGHTYELLOW = 14;
#define CONSOLE_BRIGHTWHITE = 15;

#define FILE_MISSING_COLOR 	= CONSOLE_LIGHTRED;
#define FILE_OUTDATED_COLOR = CONSOLE_LIGHTYELLOW;
#define FILE_CORRECT_COLOR 	= CONSOLE_LIGHTGREEN;

#define NORMAL_TEXT_COLOR 	= CONSOLE_BRIGHTWHITE;
#define ERROR_TEXT_COLOR 	= CONSOLE_LIGHTRED;
#define DONE_TEXT_COLOR 	= CONSOLE_LIGHTGREEN;
*/
